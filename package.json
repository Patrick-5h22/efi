{
  "name": "efi-planning",
  "version": "1.0.0",
  "description": "Planification des formations pratiques & tests EFI",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:ui": "node tests/ui/run.mjs",
    "lint": "eslint .",
    "secrets": "node scripts/check-secrets.mjs",
    "verify": "npm run lint && npm run secrets && npm test",
    "start": "python3 -m http.server 8080"
  },
  "dependencies": {
    "better-auth": "^1.7.3",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "playwright": "^1.63.0"
  }
}
